# Caterva 

## Compile and test (bash in OSX)

```
cd tests/
gcc -O -o test_name.out test_name.c -lblosc
./test_name.out
```

## Compile and debug (VSCode in OSX)

- Compile (⇧ + ⌘ + B)
- Debug (fn + F5) 
