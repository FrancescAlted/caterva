Caterva: a simple multidimensional container for compressed data
=======

Caterva is a C library on top of C-Blosc2 that implements a simple multidimensional container for compressed binary data. It adds the capability to filter, manipulate, and transform data in these containers, either in-memory or on-disk.

.. toctree::
   :maxdepth: 1
   :caption: Project API

   api
